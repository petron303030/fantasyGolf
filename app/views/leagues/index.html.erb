<p id="notice"><%= notice %></p>
  <div class = "jumbotron">
    <h2>Fantasy Golf is the most fun you and your friends have ever had playing fantasy sports</h2>
    <h3>CLick below to create a league and add your friends today to get started!</h3>
  </div>


<h1>Leagues</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Number of members</th>
      <th>Commissioner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @leagues.each do |league| %>
      <tr>
        <td><%= league.name %></td>
        <td><%= league.numMembers %></td>
        <td><%= league.commissioner %></td>
        <td><%= link_to 'Show', league, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_league_path(league), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', league, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New League', new_league_path, class: "btn btn-link" %>
